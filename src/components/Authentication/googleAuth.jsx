import React from 'react';

const GoogleAuthButton = () => {
  return (
    <a href="http://localhost:3009/auth/login/google">
      <button > Login with Google </button>
    </a>
  );
};

export default GoogleAuthButton;
